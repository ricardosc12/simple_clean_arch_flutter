// import 'package:flutter_application_1/feature/financeiro/controllers/fatura_controller.dart';
// import 'package:flutter_application_1/shared/data/dto/financeiro/financeiro_response.dart';
import 'package:flutter_test/flutter_test.dart';

// import 'utils/provider_kit.dart';

void main() {
  test("Faturas Provider Test", () {
    // final container = createContainer();
    // final faturas = await container.read(
    //   getFaturasByStatusProvider(FaturaStatus.aberta).future,
    // );
    // expect(faturas.isNotEmpty, true);
    // for (final fatura in faturas) {
    //   expect(fatura.status, FaturaStatus.aberta);
    // }
  });
}
