import 'package:flutter_application_1/feature/dashboard/controllers/docs_controllers.dart';
import 'package:flutter_test/flutter_test.dart';

import 'utils/provider_kit.dart';

void main() {
  // test("Docs Provider Test", () {
  //   final container = createContainer();
  //   final docs = container.read(docsByTypeProvider("A"));
  //   expect(docs.isNotEmpty, true);
  //   for (final doc in docs) {
  //     expect(doc.tipo, "A");
  //   }
  // });
}
